kind: pipeline
name: production

steps:
- name: deploy
  image: alpine
  commands:
  - echo deploy
  when:
    branch:
    - master
    event:
    - tag

---

kind: pipeline
name: default

steps:
  - name: build
    image: alpine
    commands:
    - echo build some code

  - name: test
    image: alpine
    commands:
    - echo run test12